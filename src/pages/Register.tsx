import { TextField } from '@mui/material';
import React from 'react';

const Register = () => {
    return (
        <div>
            <TextField></TextField>
            
        </div>
    );
};

export default Register;